# Validators

This folder contains backend input validation schemas and request validators.

Guidelines:
- Keep route-level request validation logic here.
- Keep controllers focused on orchestration and response handling.
- Reuse shared validation helpers across routes when possible.
