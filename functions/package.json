{
  "name": "travel-super-app-functions",
  "description": "Cloud Functions for Travel Super App",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test:firestore-rules": "firebase emulators:exec --only firestore --project demo-test 'mocha --timeout 30000 test/firestore-rules.test.js'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "mocha": "^11.8.0"
  }
}
